using System;
namespace vice0
{
    class Program
    { 
        static void Main(string[] args)
        {// divizorii nr n
            int n=int.Parse(Console.ReadLine());
            for(int i=1;i<n;i++)
            {
                if(n%i==0)
                {
                    Console.WriteLine($"{i} ");
                }
            }
        }
           
    }
}
