using System;
namespace vice0
{
    class Program
    { //inversam a cu b
        static void Main(string[] args)
        {
            int a = int.Parse(Console.ReadLine());
            int b = int.Parse(Console.ReadLine());
            a = a + b;
            b = a - b;
            a = a - b;
        }
           
    }
}
